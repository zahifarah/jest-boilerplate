/* 

Rename function to whatever you want, but:
1- update the name on export as well
2- update function calls accordingly in `index.test.js`, both on import but also within the tests

*/

const mainFunction = () => {
  return false
}

module.exports = mainFunction
